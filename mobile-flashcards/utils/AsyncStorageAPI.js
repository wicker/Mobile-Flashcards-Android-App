import { AsyncStorage } from 'react-native'
import initialDecks from './initialDecks'

const STORAGE_KEY = 'udacicards'

// get decks object from asyncstorage

// if it exists, parse and pass it to
// updateDecks action to update store
//
// otherwise, send initialdata to asyncstorage
// and pass initialdata to updateDecks action
// to update store

export function test() {
  return 0
}
